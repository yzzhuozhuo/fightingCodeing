/*
 给定形如 `www.toutiao.com` 的 URL，将其转换成 `com.toutiao.www` 的形式，要求必须原地操作 
 */

// function reverse (str) {
//   return str.split('.').reverse().join('.')
// }

function reverse (str) {
  
}

const str = 'www.toutiao.com'

console.log(reverse(str))